# ~/.tmuxinator/protocol_cmd_vel.yml

name: protocol_cmd_vel
root: ~/

windows:
  - joy_test:
      panes:
        - cd ~/workspace/cfr_ws/src/cfr_simulation/cfr_socket_comm/node/ && sleep 2 && ./socket_joy localhost 10000